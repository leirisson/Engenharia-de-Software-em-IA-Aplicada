# Implementation Tasks
- ler parâmetros: produto, preco_min, preco_max, top_n, salvar_planilha, planilha_url, sheet_name
- validar parâmetros e exibir mensagens claras de erro
- persistir parâmetros na sessão de execução
- expor estrutura de configuração (JSON/YAML) para reuso
