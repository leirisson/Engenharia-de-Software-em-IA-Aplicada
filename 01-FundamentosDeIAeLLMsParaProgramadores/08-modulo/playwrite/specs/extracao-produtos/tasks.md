# Implementation Tasks
- mapear seletores para nome, preço, vendedor e descrição
- implementar fallback entre variantes de seletores
- abrir cada URL e extrair detalhes
- normalizar campos (trim, remover caracteres, truncar descrição)
- retornar estrutura padrão: { produto, preco, link, vendedor, descricao }
