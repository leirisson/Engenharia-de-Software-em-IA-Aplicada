# Implementation Tasks
- inicializar projeto Node + TypeScript
- configurar tsconfig (strict, paths, outDir)
- instalar dependências: fastify, @fastify/cors, @fastify/healthcheck
- instalar prisma e @prisma/client; configurar SQLite (dev.db)
- criar prisma/schema.prisma para Session e Message
- gerar e aplicar migrations iniciais
- configurar ESLint + Prettier (scripts npm)
- instalar zod e definir schemas de validação para endpoints e use cases
- configurar testes (Vitest) com cobertura
- definir estrutura de pastas: src/domain, src/application, src/infrastructure, src/interface
- implementar Fastify app com plugins (log, cors, health)
- criar endpoints mínimos: POST /chat/session, POST /chat/message, GET /chat/history/:sessionId
- plugar validação Zod nos endpoints (body/params/query) com retorno 422 em caso de erro
- implementar repositórios Prisma para Session e Message
- implementar use cases: createSession, closeSession, appendMessage, getHistory
- escrever testes TDD para use cases e controllers
- configurar pino/console para logs estruturados
- adicionar script de execução: dev, build, start, test
