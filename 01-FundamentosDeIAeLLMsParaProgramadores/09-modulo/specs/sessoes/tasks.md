# Implementation Tasks
- RED: escrever testes Vitest para createSession, closeSession, getSession
- definir modelo Session { id, createdAt, status }
- criar repositório in-memory de sessões
- implementar service de criação, encerramento e consulta
- expor endpoints REST: POST/DELETE/GET
- GREEN: garantir que os testes passem com implementação mínima
- REFACTOR: melhorar nomes/estruturas mantendo todos os testes verdes
- testes unitários do ciclo de vida da sessão
